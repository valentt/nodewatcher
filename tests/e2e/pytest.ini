[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: Quick smoke tests
    slow: Tests that take a while to run
    admin: Tests that require admin login
    user: Tests that require user login
